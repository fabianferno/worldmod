# ==========================================================================
# CRE PROJECT SETTINGS FILE
# ==========================================================================
staging-settings:
  account:
    # The deploy key's own address (same EOA as the Sepolia contract deployer).
    # Needed for --unsigned gas estimation and for a real `deploy` to know who
    # the workflow's on-chain owner is.
    workflow-owner-address: "0x89EA57a0E61Ac9B167e263839b65E58E8DFDAAe8"
  rpcs:
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com
    # CRE's Workflow Registry + Vault DON operations (link-key, secrets, deploy)
    # resolve against ethereum-mainnet, even for a testnet workflow — so the CLI
    # needs a mainnet RPC here too. The confidential write target is still Sepolia.
    - chain-name: ethereum-mainnet
      url: https://eth.blockrazor.xyz

# ==========================================================================
production-settings:
  rpcs:
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com
    - chain-name: ethereum-mainnet
      url: https://eth.blockrazor.xyz
